

fn main() {
    // let data = (10000000, 183.19, 'Q');
    // let copy_of_data = data;
    // print!("{}, {}, {}",
    // data.0, copy_of_data.1, data.2);


    // -----Also tuples can be mutable:
    // let mut data = (10000000, 183.19, 'Q');
    // data.0 = -5;

    // ----Also the type of tuples can be made explicit:
    // let data: (i32, f64, char) = (10000000, 183.19, 'Q');

    // -------
//     let i = 0;
//     let array = [12, 13, 14];
//  let tuple = (12, 13, 14);
// //  print!("{}", array[i]);
//  print!("{}", tuple.i);  //wrong to use i 
//  // there is no way to get the value of a field of a tuple using an index determined at runtime.
//  print!("{}", tuple.0);


// ----order matters
// let data1 = (10, 'x', 12, 183.19, 'Q', false, -9);
//     let mut data2: (u16, char, i16, f64, char, bool, i16);
//     data2 = data1;



}